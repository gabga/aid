# Conventions

Project-wide technical conventions. These apply across all requirements and architecture documents. Implementation must follow these conventions unless a specific requirement explicitly overrides them.

<!-- Define your project's conventions below. Common sections include:
     - API Conventions (base URL, request/response format, error envelope, error codes, auth header, pagination, versioning)
     - Naming Conventions (endpoints, JSON fields, database tables/columns, environment variables)
     - Security Policies (password policy, token handling)
     - Date and Time format
     - ID Format
-->
